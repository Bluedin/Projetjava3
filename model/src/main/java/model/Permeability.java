package model;

/**
 * enumeration which defines 
 * if a element can be passed through
 */
public enum Permeability {

	/**
	 * 
	 */
	PENETRABLE,
	/**
	 * cannot be passed through
	 */
	BLOCKING;
	
}
